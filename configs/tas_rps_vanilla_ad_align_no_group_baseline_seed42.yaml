defaults:
    - tas_rps_vanilla_ad_align.yaml

experiment:
    name: "tas_rps_vanilla_ad_align_no_group_baseline_seed42"
    base_seed: 42

trainers:
    agent_trainer:
        kwargs:
            no_rloo_grouping: True
